build-ssa-harbor-image-cleaner:
  tags:
    - piximos
  rules:
    - if: $CI_COMMIT_REF_NAME =~ /^(master)$/
      changes:
        - scripted-simple-atoms/scripted-simple-atoms-base/*
        - scripted-simple-atoms/harbor-image-cleaner-atom/*
  stage:
    build-scripted-simple-atoms
  variables:
    IMAGE_NAME: "scriptedatom/ssa-harbor-image-cleaner"
    DOCKER_IMAGE_PATH: "./scripted-simple-atoms/harbor-image-cleaner-atom/Dockerfile"
    DOCKER_BUILD_CONTEXT: "./scripted-simple-atoms/harbor-image-cleaner-atom/."
    SA_BASE_VERSION: ${ssa_base_version}
    IMAGE_VERSION: ${ssa_harbor_image_cleaner_version}
  script:
    - ./ci-cd/scripts/build-public-image.sh
  needs:
    - build-ssa-base

ssa-harbor-image-cleaner-slacker:
  tags:
    - piximos
  rules:
    - if: $CI_COMMIT_REF_NAME =~ /^(master)$/
      when: on_success
      variables:
        SUCCESS: "true"
    - if: $CI_COMMIT_REF_NAME =~ /^(master)$/
      when: on_failure
      variables:
        SUCCESS: "false"
  variables:
    IMAGE: "scriptedatom/ssa-harbor-image-cleaner"
    TAGS: "latest ${ssa_harbor_image_cleaner_version}"
  stage:
    slack-broadcast
  needs:
    - build-ssa-harbor-image-cleaner
  script:
    - ./ci-cd/scripts/pipeline-slacker.sh

ssa-harbor-image-cleaner-discorder:
  tags:
    - piximos
  rules:
    - if: $CI_COMMIT_REF_NAME =~ /^(master)$/
      when: on_success
      variables:
        SUCCESS: "true"
    - if: $CI_COMMIT_REF_NAME =~ /^(master)$/
      when: on_failure
      variables:
        SUCCESS: "false"
  variables:
    IMAGE: "scriptedatom/ssa-harbor-image-cleaner"
    TAGS: "latest ${ssa_harbor_image_cleaner_version}"
  stage:
    discord-broadcast
  needs:
    - build-ssa-harbor-image-cleaner
  script:
    - ./ci-cd/scripts/pipeline-discorder.sh

ssa-harbor-image-cleaner-cleaner:
  tags:
    - piximos
  rules:
    - if: $CI_COMMIT_REF_NAME =~ /^(master)$/
      when: on_success
  variables:
    IMAGE: "scriptedatom/ssa-harbor-image-cleaner"
    TAGS: "latest ${ssa_harbor_image_cleaner_version}"
  stage:
    cleaner
  needs:
    - build-ssa-harbor-image-cleaner
  script:
    - ./ci-cd/scripts/pipeline-cleaner.sh
