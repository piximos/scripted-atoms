build-ssa-base:
  tags:
    - piximos
  rules:
    - if: $CI_COMMIT_REF_NAME =~ /^(master)$/
      changes:
        - scripted-simple-atoms/scripted-simple-atoms-base/*
        - scripted-simple-atoms/**/*
  stage:
    build-bases
  variables:
    IMAGE_NAME: "scriptedatom/ssa-base"
    DOCKER_IMAGE_PATH: "./scripted-simple-atoms/scripted-simple-atoms-base/Dockerfile"
    DOCKER_BUILD_CONTEXT: "./scripted-simple-atoms/scripted-simple-atoms-base/."
    SA_BASE_VERSION: ${ssa_base_version}
    IMAGE_VERSION: ${ssa_base_version}
  script:
    - ./ci-cd/scripts/build-public-image.sh
  needs:
    - validate-branch-name

ssa-base-slacker:
  tags:
    - piximos
  rules:
    - if: $CI_COMMIT_REF_NAME =~ /^(master)$/
      when: on_success
      variables:
        SUCCESS: "true"
    - if: $CI_COMMIT_REF_NAME =~ /^(master)$/
      when: on_failure
      variables:
        SUCCESS: "false"
  variables:
    IMAGE: "scriptedatom/ssa-base"
    TAGS: "latest ${ssa_base_version}"
  stage:
    slack-broadcast
  needs:
    - build-ssa-base
  script:
    - ./ci-cd/scripts/pipeline-slacker.sh

ssa-base-discorder:
  tags:
    - piximos
  rules:
    - if: $CI_COMMIT_REF_NAME =~ /^(master)$/
      when: on_success
      variables:
        SUCCESS: "true"
    - if: $CI_COMMIT_REF_NAME =~ /^(master)$/
      when: on_failure
      variables:
        SUCCESS: "false"
  variables:
    IMAGE: "scriptedatom/ssa-base"
    TAGS: "latest ${ssa_base_version}"
  stage:
    discord-broadcast
  needs:
    - build-ssa-base
  script:
    - ./ci-cd/scripts/pipeline-discorder.sh

ssa-base-cleaner:
  tags:
    - piximos
  rules:
    - if: $CI_COMMIT_REF_NAME =~ /^(master)$/
      when: on_success
  variables:
    IMAGE: "scriptedatom/ssa-base"
    TAGS: "latest ${ssa_base_version}"
  stage:
    cleaner
  needs:
    - build-ssa-base
  script:
    - ./ci-cd/scripts/pipeline-cleaner.sh
