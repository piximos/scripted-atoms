{
  "variables": [
    {
      "name": "SA_REDIS_HOST",
      "desc": "The Redis database host.",
      "required": true
    },
    {
      "name": "SA_REDIS_PORT",
      "desc": "The Redis database port.",
      "required": true
    },
    {
      "name": "SA_REDIS_PASSWORD",
      "desc": "The Redis database password.",
      "required": false
    }
  ]
}